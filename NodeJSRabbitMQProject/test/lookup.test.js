const assert = require('assert');
const xmlhttp = require('xmlhttprequest');

describe('Account Lookup Service', () => {
  it('starts', done => {
    /*var exec = require('child_process').exec;

    child = exec(
      'java -jar ../SpringBootProject/target/Lookup-0.0.1-SNAPSHOT.jar',
      function(error, stdout, stderr) {
        console.log(error);
        assert(error == null);
        done();
      }
    );*/
    done();
  });

  it('gets an account', done => {
    /*var XMLHttpRequest = xmlhttp.XMLHttpRequest;
    var xmlHttp = new XMLHttpRequest();
    xmlHttp.onreadystatechange = function() {
      console.log(xmlHttp);
      assert(xmlHttp.readyState == 4 && xmlHttp.status == 200);
      done();
    };
    xmlHttp.open(
      'GET',
      'http://127.0.0.1:8080/Lookup?providerName=Walmart',
      true
    );
    xmlHttp.send(null);*/
    //assert(false);
    done();
  });
});
